# OpsBay Worker Configuration
# Copy this file to /opt/opsbay-cron-broker/worker.env and customize

# Broker API endpoint
API=http://broker.example.com:8080

# Unique identifier for this worker node
NODE_ID=worker-node-1

# Shared secret for HMAC authentication (must match broker)
SECRET=your-shared-secret-here

# Polling interval in seconds (default: 5)
POLL_INTERVAL=5

# Maximum retry attempts for failed requests (default: 3)
MAX_RETRIES=3

# Optional: Custom timeout for job execution (seconds)
# JOB_TIMEOUT=300

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=info